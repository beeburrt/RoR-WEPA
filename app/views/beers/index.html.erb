<p style="color: green"><%= notice %></p>

<h1>Beers</h1>

<%= link_to "Add New Beer", new_beer_path %>

<div id="beers">
  <% @beers.each do |beer| %>
    <%= render beer %>
    <hr/>
  <% end %>
</div>
